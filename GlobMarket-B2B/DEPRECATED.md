# Legacy — solo referencia

El código activo del portal B2B está en:

- API: `apps/portal/gmbackend/`
- Web: `apps/portal/web/`

Arranque: `python run.py` desde la raíz del monorepo o Docker en `infrastructure/compose/`.

Esta carpeta ya no es el punto de entrada del proyecto.
